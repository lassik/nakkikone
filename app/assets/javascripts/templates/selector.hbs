<div id="party-creation-dialog" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="partyCreationLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="partyCreationLabel">Create a new party</h3>
  </div>
  <div class="modal-body">
    <label for="party-title-input">Party name: <input id="party-title-input" type="text" name="title" value=""></label>
    <ul>
      <li>Don't append the date to the party name, it will be appended automatically.</li>
      <li>Don't use '/' (slahs) or '.' (dot) in the title.</li>
    </ul>
  </div>
  <div class="modal-footer">
    <button id="creator" class="btn btn-primary" data-dismiss="modal" aria-hidden="true">Create party</button>
  </div>
</div>
  <button class="btn btn-success pull-left span2" href="#party-creation-dialog" role="button" data-toggle="modal">New Party</button>
	<form method="post" class="span8">
	  <div class="controls controls-row">
	    <select name="parties" class="selector" style="margin-bottom: 0px;">
	      <option value="">select one to edit</option>
	      {{#each parties}}
	      <option value="{{ this.cid }}">{{ this.title }}</option>
	      {{/each}}
	    </select>
	</form>
  <button class="deletor btn btn-danger pull-right">Delete selected</button>
